# Shujaat Music Player

A simple Flask‑based web application that integrates with the Spotify API
to let users browse and control their music.

## 🔧 Features

- Authenticate with Spotify
- View playlists / tracks
- Play, pause or skip songs
- Simple HTML interface using `templates/index.html`

## 🚀 Getting Started

### Requirements

- Python 3.7+
- `pip` (or a virtual environment tool such as `venv`/`virtualenv`)
- A Spotify Developer account and application (client ID & secret)

### Installation

```sh
# clone the repo (if you haven’t already)
git clone <repo-url>
cd spotify_app

# create & activate a virtual environment
python -m venv venv
venv\\Scripts\\activate     # Windows
# or `source venv/bin/activate` on macOS/Linux

# install dependencies
pip install -r [requirements.txt](http://_vscodecontentref_/1)
```
